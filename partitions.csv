# Name,   Type, SubType, Offset,  Size, Flags
# Dual-OTA layout for 4MB flash. nvs keeps its built-in offset/size so
# wifi creds, hostname, motor cal, V/Hz tuning, slew, pump range and
# pole pairs survive the partition swap. otadata replaces the old phy_init
# slot at 0xf000; phy_init shifts to 0x11000. Two 1.875M app slots leave
# comfortable headroom over the ~0.9M binary; first OTA after a single-app
# flash will land in ota_0 via esp_ota_get_next_update_partition().
nvs,      data, nvs,     0x9000,  0x6000,
otadata,  data, ota,     0xf000,  0x2000,
phy_init, data, phy,     0x11000, 0x1000,
ota_0,    app,  ota_0,   0x20000, 0x1E0000,
ota_1,    app,  ota_1,   ,        0x1E0000,
